{
  "version": 2,
  "installCommand": "npm install --no-audit --no-fund --legacy-peer-deps --no-package-lock",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
